spring:
    application:
        name: config-server

cloud:
    config:
        server:
            git:
                uri: https://github.com/mehmetfatihkavala/config-repo.git
                default-label: main
                search-paths:
                    - configurations/
                    - congigurations/{application}

server:
    port: 7878                    