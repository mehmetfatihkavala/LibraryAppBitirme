spring:
    application:
        name: config-server

cloud:
    config:
        server:
            git:
                uri: https://github.com/mehmetfatihkavala/LibraryAppBitirme.git
                default-label: main
                search-paths:
                    - configurations/
                    - congigurations/{application}

server:
    port: 8787                    